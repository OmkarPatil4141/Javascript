"use strict" //we are telling interpreter to treat code as a newer version

// alert(3+3)  //we are using node not browser

/*
alert (3         //code must me readable
+
3) */

//data types
// 1.primitive datatypes
let name = "omkar";  //string

let age = 21;       //number -> range 2 to power 53 
                    //if we have to use big than range then we use bigint

let isLoggedIn = false;

let maychange = null; // it is standalone value (it is object)

let state; //it is undefined => we had not stored any value 

// 2. object

console.log(typeof {});
console.log(typeof null)

console.log(typeof undefined)
